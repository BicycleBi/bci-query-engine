schema_version: 1
client: rf
environment: dev
source_repo: .
source_commit: HEAD

targets:
  stack_dir: /home/shared/bci-clients/rfdev
  compose_project: rfdev

service_runtime:
  promote:
    source: .
    service: query-engine
    build_context_env_var: QUERY_ENGINE_BUILD_CONTEXT
    purpose: RF Dev database-owned filtered CSV outputs and reusable artifact distribution groups

validate:
  - type: compose-config
  - type: service-health
    service: query-engine
